/**
 * config.example.js — Plantilla para desarrollo.
 * Copia este archivo como `config.js` y coloca tu API Key real.
 * `config.js` está ignorado por Git para no exponer tus credenciales.
 */
export default {
    // Tu API Key (v3 auth) de TMDB. Regístrate en:
    // https://www.themoviedb.org/settings/api
    TMDB_API_KEY: '',
};